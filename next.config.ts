import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    /* config options here */
    images: {
        domains: ['lf16-co.g-p-static.com', 'p16-amd-va.tiktokcdn.com', 'p16-sign-sg.tiktokcdn.com', 'p16-sign-va.tiktokcdn.com'],
    },
};

export default nextConfig;
